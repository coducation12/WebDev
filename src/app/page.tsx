"use client";

import HubPortal from "@/components/HubPortal";

/**
 * Roadmap Step 2: Core Navigation Hub (Home)
 * Clean, simple interface focusing on the 7 design references.
 */
export default function Home() {
  return (
    <main className="min-h-screen bg-white dark:bg-bora-dark flex items-center justify-center p-6 sm:p-12">
      <HubPortal />
    </main>
  );
}
